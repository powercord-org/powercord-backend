/* eslint-disable */
module.exports = {
  // No need for pagination here, as it's admin only. Raw listing will be enough
  listEntities: (type) => (req, res) => {

  },

  createEntity: (type) => (req, res) => {

  },

  updateEntity: (type) => (req, res) => {

  },

  deleteEntity: (type) => (req, res) => {

  }
};
